# ts-rs bindings land in the committed src/gen/ (relative to src-tauri/),
# so a plain `cargo test` regenerates them in place — no env var to remember.
[env]
TS_RS_EXPORT_DIR = { value = "../src/gen", relative = true }
